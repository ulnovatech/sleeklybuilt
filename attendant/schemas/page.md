# page schema

Companion notes for [page.json](page.json).

Required: `page_id`, `path`, `title`, `external`, `section_ids`.

`path` must start with `/`. `section_ids` is an array of strings. `external` is true only for `/portfolio-app/`.

Optional: `default_service_id`, `default_product_kind`.

`additionalProperties`: false.
